[project]
name = "workshop"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "marimo>=0.23.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
