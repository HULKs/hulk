stages:
  sample-full-positives:
    cmd: annotation-sampler --image-color-space RGB corrected.json full-positives.tfrecord
    deps:
    - corrected.json
    - raw
    outs:
    - full-positives.tfrecord
