[project]
name = "nao-env"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.10"

dependencies = ["gymnasium", "numpy", "nao_interface", "throwing", "rewards"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
