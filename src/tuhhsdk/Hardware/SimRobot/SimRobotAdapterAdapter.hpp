#pragma once


class SimRobotAdapterAdapter
{
public:
  /**
   * @brief getSimulatedTime returns the simulated time
   * @return the simulated milliseconds since the start of the simulation
   */
  static unsigned int getSimulatedTime();
};
