- High: KeepAll QoS creates effectively unbounded queues. subscriber_queue_capacity returns usize::MAX for KeepAll, and BoundedQueue only drops when len >= capacity, so slow subscribers/service servers can grow memory without bound. See crates/ros-z/src/pubsub/subscriber.rs:22, crates/ros-z/src/pubsub/subscriber.rs:284, crates/ros-z/src/service.rs:473, crates/ros-z/src/queue.rs:22.
- High: action result waiters can hang forever after goal expiration. handle_get_result registers oneshot waiters for non-terminal goals, but expire_goals removes goals without notifying or clearing result_futures, leaving tasks stuck until server shutdown. See crates/ros-z/src/action/driver.rs:239, crates/ros-z/src/action/driver.rs:263, crates/ros-z/src/action/server.rs:1124.
- High: action clients leak active goal entries if a GoalHandle is dropped without calling result/result_async. active_goals is inserted on send, but cleanup only happens on result consumption or rejection/call failure. Dropped or cancelled handles can leave unbounded feedback/status senders alive. See crates/ros-z/src/action/client.rs:641, crates/ros-z/src/action/client.rs:817, crates/ros-z/src/action/client.rs:940.
- High: SHM serialization can publish the full allocation, not just written bytes. ShmWriter::into_zbuf returns ZBuf::from(self.buffer) despite the doc saying only 0..position is included, and publisher ignores actual_size except tracing. This can expose trailing padding/garbage to raw subscribers/recording paths. See crates/ros-z/src/shm.rs:372, crates/ros-z/src/shm.rs:380, crates/ros-z/src/pubsub/publisher.rs:590.
- High: SHM serialization panics on underestimated sizes instead of falling back. SerdeCdrWireCodec::serialized_size_hint is size_of::<T>() * 2 + 4, which underestimates common dynamic payloads, and ShmWriter::write_bytes asserts on overflow. Since publish only catches Err, not panics, this can take down a publisher. See crates/ros-z/src/msg.rs:298, crates/ros-z/src/msg.rs:302, crates/ros-z/src/shm.rs:393, crates/ros-z/src/pubsub/publisher.rs:566.
- High: FutureQueueSubscriber::safe_time uses the lexicographically first publication key, not the oldest in-flight time. inflight is keyed by (EndpointGlobalId, sequence_number), so concurrent publishers can advance safe time past older in-flight data and corrupt future-map persistence boundaries. See crates/ros-z-streams/src/future_queue.rs:103, crates/ros-z-streams/src/future_queue.rs:114.
- Medium: generated bounded sequence encoders panic on invalid user data. The generated code uses assert! for len <= bound, while the public encode path returns Result, so publishing an over-bound message unwinds instead of returning a serialization error. See crates/ros-z-codegen/src/generator/rust.rs:315, crates/ros-z/src/msg.rs:538.
- Medium: bounded string constraints are advertised in schema but not enforced in generated Rust encoding. string<=N becomes FieldType::BoundedString(N), but single string encoding delegates directly to CdrEncode, allowing bytes that violate the schema contract. See crates/ros-z-codegen/src/generator/rust.rs:281, crates/ros-z-codegen/src/generator/rust.rs:1205.
- Medium: generated estimated_serialized_size undercounts strings by one byte per string. CDR writes a null terminator, but size estimation adds only 4 + len; arrays/sequences with enough strings can exceed the per-field padding slack and fail SHM tests or user-provided SHM allocations. See crates/ros-z-codegen/src/generator/rust.rs:1293, crates/ros-z-msgs/src/size_estimation.rs:125, crates/ros-z-cdr/src/primitives.rs:143.
- Low: ros-z echo --timeout can panic on invalid floats. Duration::from_secs_f64 is called without checking positive finite input, unlike record which validates it. See crates/ros-z-cli/src/commands/echo.rs:42, crates/ros-z-cli/src/commands/record.rs:100.


- refactor schema/schema.rs -> more readable, very long file
- there is a lot of machinery on schema.rs to just parse Rust to validate correct type names. This is not needed in any implementation. This is a ficticious requirement. There is nothing against having a type name that is not Rust parsable.
- bump zenoh
