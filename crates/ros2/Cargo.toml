[package]
name = "ros2"
version.workspace = true
edition.workspace = true
license.workspace = true
homepage.workspace = true

[dependencies]
pyo3 = { workspace = true, optional = true }
ros-z-msgs = { workspace = true, features = [
  "sensor_msgs",
  "geometry_msgs",
  "std_msgs",
] }

[features]
pyo3 = ["dep:pyo3"]
